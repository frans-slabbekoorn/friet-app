import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    flatList: {
        position: 'absolute',
        top: 0,
        width: '100%',
        height: '100%',
        flex: 1,
    },
});
